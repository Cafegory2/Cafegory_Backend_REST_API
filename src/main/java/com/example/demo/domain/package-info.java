/**
 * <p>
 *     도메인 클래스들이 속한 패키지이다.
 * </p>
 * <p>
 *     이 패키지에 속한 도메인 클래스의 public 메서드를 이용해 데이터를 갱신한다. 이 패키지에 속한 클래스의 인스턴스는 내부에 다른 도메인 클래스의 인스턴스나
 *     entity 패키지에 속한 클래스의 인스턴스를 참조할 수 있다. 이를 이용해 도메인 로직을 수행한다.
 * </p>
 */
package com.example.demo.domain;
