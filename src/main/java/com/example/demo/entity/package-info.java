/**
 * JPA를 이용한 영속성 엔티티를 표현한 클래스가 속한 클래스다.
 * <p>
 *     이 프로젝트에선 영속성 엔티티 인스턴스에 도메인 로직을 구현하는 것이 아니라, 단순히 setter 및 getter 를 이용한다. 도메인 로직을 표현하는 메서드는
 *     도메인 클래스에 정의한다. 장기적으로 도메인 로직과 영속성 기술을 분리할 수 있게 하기 위함이다.
 * </p>
 */

package com.example.demo.entity;